{
  "name": "secret-santa-coordinator-v1",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply secret-santa-coordinator-v1 --local",
    "db:migrate:remote": "wrangler d1 migrations apply secret-santa-coordinator-v1 --remote",
    "test": "node --test",
    "check": "node --check src/worker.js && node --check public/app.js"
  },
  "devDependencies": {
    "wrangler": "^4.31.0"
  }
}
